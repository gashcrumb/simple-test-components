import { simpleTestComponentsPlugin } from './plugin';

describe('simple-test-components', () => {
  it('should export plugin', () => {
    expect(simpleTestComponentsPlugin).toBeDefined();
  });
});
